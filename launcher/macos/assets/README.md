# macOS application icon

`CrocoblockSiteFactory.icns` is the configured release icon input. The approved
binary asset must be supplied by the release owner before a macOS package can
be built. The build fails closed when the file is absent; it does not generate
or substitute an unapproved icon.
